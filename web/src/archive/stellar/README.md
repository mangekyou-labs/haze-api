# Archived Stellar web surface

The files in this directory preserve the historical Stellar/Soroban checkout,
evaluation, ticket-ledger, and browser-credential UI for reference. They are
excluded from the active TypeScript build and are not routable by Next.js.

The active product uses Stripe-only purchases, Base Sepolia status, encrypted
browser credential exports, and optional SIWE wallet linking.
