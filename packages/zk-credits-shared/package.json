{
  "name": "@zk-credits/shared",
  "version": "0.1.0",
  "description": "Isomorphic crypto + proof helpers shared by the zk-api-credits browser (web) and Node (gateway/CLI) runtimes.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src"],
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@scure/bip39": "^2.2.0",
    "snarkjs": "^0.7.6"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
